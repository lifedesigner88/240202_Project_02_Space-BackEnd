package com.encore.space.domain.chat.domain;

public enum MessageType {
    CHAT,   // when the user sends a message
    JOIN,   // when new user joins the chat
    LEAVE   // when user leaves the chat
}
